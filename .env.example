# Local web address: http://127.0.0.1:8000
CAREER_QUEST_PORT=8000
DEMO_MODE=true

# Use a unique value with 32 or more characters before any non-demo use.
SESSION_SECRET=replace-with-a-long-random-session-secret

# Leave all three empty for verified rules-based recommendations.
# Set all three together for an OpenAI-compatible chat-completions endpoint.
LLM_API_URL=
LLM_API_KEY=
LLM_MODEL=
